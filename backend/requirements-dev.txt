# Development dependencies — install with:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8,<9
pytest-asyncio>=0.23,<1
httpx2>=2,<3
ruff>=0.6
mypy>=1.11
# Embedded PostgreSQL for tests where no DATABASE/test server is available
# (CI, sandboxes). On a dev machine with `docker compose up -d db` you can
# set TEST_DATABASE_URL/TEST_ADMIN_DATABASE_URL instead.
pgserver>=1.3,<2
